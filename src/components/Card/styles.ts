import { StyleSheet } from "aphrodite";
import colors from "../../colors";

const styles = StyleSheet.create({
  cardComponent: {
    marginTop: 12,
    marginBottom: 12,
    padding: 24,
    borderRadius: 12,
    boxShadow: "none",
    border: "2px solid gray",
    borderColor: colors.six
  }
});

export default styles;
